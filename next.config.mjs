
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/lycanwebsite',
  assetPrefix: '/lycanwebsite',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
